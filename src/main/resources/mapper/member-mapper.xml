<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dmu.dasom.dasom_homepage.repository.MemberRepository">

    <select id="existByEmail" parameterType="String" resultType="boolean">
        SELECT EXISTS (SELECT * FROM member_table WHERE mem_email = #{memEmail}) AS result
    </select>

    <insert id="saveNewMember" parameterType="DasomMember">
        INSERT INTO member_table (mem_email, mem_password, mem_name, mem_grade, mem_department, mem_rec_no)
        VALUES (#{memEmail}, #{memPassword}, #{memName}, #{memGrade}, #{memDepartment}, #{memRecNo})
    </insert>

    <select id="getMemberByEmail" parameterType="String" resultType="DasomMember">
        SELECT * FROM member_table WHERE mem_email = #{memEmail}
    </select>

</mapper>