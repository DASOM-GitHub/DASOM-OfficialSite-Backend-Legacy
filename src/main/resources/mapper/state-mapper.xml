<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dmu.dasom.dasom_homepage.repository.AdminRepository">

    <delete id="deleteMember" parameterType="int">
        DELETE FROM member_table where mem_no = #{memNo}
    </delete>

    <update id="modifyMember" parameterType="MemberState">
        UPDATE member_table set mem_department = #{memDepartment} , mem_name = #{memName} , mem_contact = #{memContact}
        where memno = #{memNO}
    </update>

    <update id="stateChange" parameterType="MemberState">
        UPDATE member_table set mem_state = #{memState} where memno = #{memNO}
    </update>

    <select id="getMemberList" resultType="MemberState">
        SELECT mem_no, mem_name, mem_state FROM member_table
    </select>
</mapper>